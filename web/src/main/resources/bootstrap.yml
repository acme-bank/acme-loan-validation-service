spring:
  cloud:
    config:
      uri: http://config.acme.com
      failFast: true
      name: acme-loan-validation
      profile: dev
      label: develop

---

spring:
  profiles: test
  cloud:
    config:
      profile: test

---

spring:
  profiles: prod
  cloud:
    config:
      profile: prod
